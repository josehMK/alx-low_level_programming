#include "main.h"
/**
 * is_prime_number - determines if an integer is a prime number
 * @n: number to check
 * return: 1 if the input is a prime number, 0 if otherwise
 */

int _is_prime_number(int n)
{
